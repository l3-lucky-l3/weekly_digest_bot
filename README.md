# Weekly Digest Bot 🤖

Бот для мониторинга чатов сообщества и автоматического создания еженедельных дайджестов с использованием AI.

## Возможности ✨

- **Мониторинг чатов** - отслеживание сообщений в группах и каналах
- **AI-анализ** - автоматическая обработка контента с помощью моделей OpenRouter
- **Расписание постов**:
  - 📅 Понедельник 10:00 - цели и блокеры недели
  - 📊 Пятница 19:00 - Weekly Digest
- **Мультимодельность** - поддержка нескольких AI моделей с автоматическим переключением
- **Управление через команды** - простой интерфейс для настройки

## Установка и настройка 🚀

### Предварительные требования
- Docker и Docker Compose
- Telegram Bot Token
- OpenRouter API Key

### 1. Клонирование репозитория
```bash
git clone <repository-url>
cd <repository-name>
```

### 2. Настройка окружения
```bash
cp .env.example .env
# Редактируем .env - добавляем BOT_TOKEN, OPENROUTER_API_KEY, CHANNEL_ID
```

### 3. Запуск контейнера
```bash
docker-compose up -d
```

### 4. Просмотр логов
```bash
docker-compose logs -f
```

### 5. Остановка
```bash
docker-compose down
```

## Настройка бота в Telegram 🤖

### Создание бота через BotFather
1. Найти @BotFather в Telegram
2. Команда `/newbot`
3. Указать имя и username бота
4. Получить токен и добавить в `.env`

### Настройки BotFather (рекомендуемые)
```
✅ Allow Groups
✅ Allow Channels
❌ Group Privacy Mode (ВЫКЛЮЧЕН для получения всех сообщений)
```

## Конфигурация .env ⚙️

```env
BOT_TOKEN=your_telegram_bot_token_here
OPENROUTER_API_KEY=your_openrouter_api_key_here
CHANNEL_ID=@your_channel_username_or_id
```

## OpenRouter API 🔑

### Получение API ключа
1. Регистрация на https://openrouter.ai/
2. Перейти в настройки: https://openrouter.ai/settings/keys
3. Создать новый API ключ
4. Добавить ключ в `.env` файл

### Бесплатные модели
Доступные бесплатные модели:  
https://openrouter.ai/models?order=pricing-low-to-high&q=free

**Популярные бесплатные модели:**
- `nvidia/nemotron-nano-12b-v2-vl:free`
- `openai/gpt-oss-20b:free` 
- `deepseek/deepseek-chat-v3.1:free`
- `qwen/qwen3-coder:free`

## Использование бота 📋

### Основные команды

| Команда                     | Описание                    |
|-----------------------------|-----------------------------|
| `/start`                    | Начальная информация о боте |
| `/get_chat_id`              | Показать ID текущего чата   |
| `/add_chat <id>`            | Добавить чат в мониторинг   |
| `/remove_chat <id>`         | Удалить чат из мониторинга  |
| `/list_chats`               | Список отслеживаемых чатов  |
| `/add_model <name> <model>` | Добавить AI модель          |
| `/remove_model <name>`      | Удалить AI модель           |
| `/models`                   | Список доступных моделей    |

### Добавление чатов в мониторинг

**Для публичных чатов/каналов:**
- Перешлите любое сообщение из чата боту
- Используйте показанный ID с командой `/add_chat`

**Для приватных чатов:**
- Добавьте бота в чат как администратора
- Используйте `/get_chat_id` для получения ID
- Используйте `/add_chat <id>` для добавления

### Добавление AI моделей

```bash
# Пример добавления модели
/add_model nvidia nvidia/nemotron-nano-12b-v2-vl:free
/add_model openai openai/gpt-oss-20b:free 
```

## Структура проекта 📁

```
weekly-digest-bot/
├── data/
│   └── database.db        # База данных (создается автоматически)
│
├── src/                   # Исходный код:
│   ├── __init__.py        # -Инициализация пакета Python (обязательный файл)
│   ├── main.py            # -Основной файл бота
│   ├── ai_client.py       # -Клиент для работы с AI
│   └── db.py              # -Работа с базой данных
│
├── docker-compose.yml     # Конфигурация Docker
├── Dockerfile             # Образ Docker
├── .env.example           # Шаблон переменных окружения
└── README.md              # Документация
```

## Решение проблем 🔧

### Бот не получает сообщения в группах
- Проверьте что Group Privacy Mode ВЫКЛЮЧЕН в BotFather
- Убедитесь что бот добавлен как администратор

### Ошибки AI моделей
- Проверьте доступность модели на OpenRouter
- Добавьте несколько моделей для резервирования
- Проверьте баланс на OpenRouter (для платных моделей)

### Проблемы с Docker
```bash
# Пересборка контейнера
docker-compose down
docker-compose up -d --build

# Очистка volumes (при проблемах с БД)
docker-compose down -v
```
