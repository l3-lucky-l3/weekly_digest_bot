# 🤖 Weekly Digest Bot - AI-бот для дайджестов сообщества

**PoC (Proof of Concept)** бот для автоматического сбора, семантического анализа и публикации структурированных дайджестов в Telegram-сообществе.

## 🎯 Возможности

- **📥 Автоматический сбор сообщений** из настроенных топиков
- **🧠 AI-классификация** на цели (goals) и блокеры (blockers) 
- **🕒 Автопостинг по расписанию**:
  - Понедельник 10:00 - цели и блокеры недели (в топик Conductor)
  - Пятница 19:00 - Weekly Digest (в топик Анонсы)
- **🔀 Мультимодельность** - поддержка нескольких AI моделей через OpenRouter
- **⚙️ Простое управление** через Telegram-команды

## 🚀 Быстрый старт

### 🛠️ Автоматическая установка (рекомендуется)

Для быстрой установки используйте наш автоматический скрипт:

```bash
# Скачайте установочный скрипт
curl -O https://raw.githubusercontent.com/jbShanti/structme.weekly-digest.bot/main/install-bot.sh
chmod +x install-bot.sh

# Запустите установку
./install-bot.sh install
```

**Скрипт автоматически:**
- ✅ Проверит и установит Docker (если нужно)
- ✅ Скачает или обновит репозиторий  
- ✅ Настроит .env файл в интерактивном режиме
- ✅ Запустит все сервисы в Docker

**Дальнейшие команды:**
```bash
# Обновление бота
./install-bot.sh update

# Просмотр логов
docker-compose logs -f telegram-bot
```

### ⚙️ Ручная установка (альтернатива)

Если вы предпочитаете ручную установку:

**Предварительные требования:**
- Docker и Docker Compose
- Токен Telegram бота
- API ключ OpenRouter

```bash
# 1. Клонирование репозитория
git clone https://github.com/jbShanti/structme.weekly-digest.bot.git
cd structme.weekly-digest.bot

# 2. Настройка окружения
cp .env.example .env
# Отредактируйте .env файл:
# BOT_TOKEN=your_telegram_bot_token
# OPENROUTER_API_KEY=your_openrouter_api_key  
# MAIN_CHAT_ID=your_main_chat_id

# 3. Запуск в Docker
docker-compose up -d
```

### 🎯 Первоначальная настройка

После запуска бота выполните в Telegram:
1. `/addtopic` - в каждом топике для мониторинга
2. `/selectconductortopic` - в топике для понедельничных постов  
3. `/selectanouncestopic` - в топике для пятничных дайджестов

## ⚙️ Настройка бота в Telegram

### Создание бота через @BotFather
1. Найти `@BotFather` в Telegram
2. Команда `/newbot`
3. Указать имя и username бота
4. Получить токен и добавить в `.env`

### Важные настройки BotFather:
```
✅ Allow Groups - РАЗРЕШЕНО
✅ Allow Channels - РАЗРЕШЕНО  
❌ Group Privacy Mode - ВЫКЛЮЧЕН (для получения всех сообщений)
```

## 🎛️ Команды управления

### Основные команды
| Команда        | Описание                         |
|----------------|----------------------------------|
| `/start`       | Информация о боте                |
| `/get_chat_id` | Показать ID текущего чата/топика |
| `/showconfig`  | Текущая конфигурация             |

### Управление топиками (выполнять внутри топика!)
| Команда                 | Описание                                  |
|-------------------------|-------------------------------------------|
| `/addtopic`             | Добавить текущий топик в источники        |
| `/deletetopic`          | Удалить топик из источников               |
| `/listtopics`           | Список топиков-источников                 |
| `/selectconductortopic` | Установить топик для постов в понедельник |
| `/selectanouncestopic`  | Установить топик для дайджестов в пятницу |

### Управление AI моделями
| Команда                     | Описание                 |
|-----------------------------|--------------------------|
| `/add_model <name> <model>` | Добавить AI модель       |
| `/remove_model <name>`      | Удалить AI модель        |
| `/models`                   | Список доступных моделей |

### Утилиты
| Команда              | Описание                          |
|----------------------|-----------------------------------|
| `/test_post monday`  | Тест понедельничного поста        |
| `/test_post friday`  | Тест пятничного дайджеста         |
| `/cleanup_messages`  | Очистка старых сообщений из БД    |

**Пример добавления модели:**
```bash
/add_model nvidia nvidia/nemotron-nano-12b-v2-vl:free
/add_model openai openai/gpt-oss-20b:free
```

## 🔑 Получение OpenRouter API ключа

1. Зарегистрируйтесь на [https://openrouter.ai/](https://openrouter.ai/)
2. Перейдите в настройки: [https://openrouter.ai/settings/keys](https://openrouter.ai/settings/keys)
3. Создайте новый API ключ
4. Добавьте ключ в `.env` файл

### 🆓 Бесплатные модели OpenRouter [тут](https://openrouter.ai/models?order=pricing-low-to-high&q=free)

## 🏗️ Архитектура проекта

```
structme.weekly-digest.bot/
├── data/                   # База данных (создается автоматически)
│   └── database.db
│
├── src/                    # Исходный код
│   ├── handlers/           # Обработчики
│   │   ├── __init__.py
│   │   ├── commands.py     # Обработчики команд
│   │   ├── messages.py     # Обработка сообщений
│   │   └── topics.py       # Управление топиками
│   │
│   ├── utils/              # Вспомогательные модули
│   │   ├── __init__.py
│   │   └── filters.py      # Кастомные фильтры
│   │
│   ├── __init__.py
│   ├── ai_client.py        # Клиент для работы с AI (OpenRouter)
│   ├── db.py               # Работа с SQLite базой
│   └── main.py             # Основной модуль бота
│
├── prompts/                # Промпты для AI
│   ├── __init__.py
│   ├── monday.md           # Промпт для понедельника
│   └── friday.md           # Промпт для пятницы
│
├── .env                    # Файл переменных окружения
├── .env.example            # Шаблон переменных окружения
├── docker-compose.yaml     # Конфигурация Docker
├── Dockerfile              # Образ Docker
├── README.md               # Документация
└── requirements.txt        # Зависимости
```

## 🔄 Трехступенчатая классификация

Бот использует интеллектуальную систему обработки сообщений:

1. **📎 Наследование** - если сообщение является ответом, наследует thread_id родителя
2. **🔗 Семантический слинг** - AI проверяет связь с существующими тредами
3. **🏷️ Новая классификация** - если связей нет, создается новый тред (goal/blocker)

## 🛠️ Решение проблем

### Бот не видит сообщения
- Убедитесь что **Group Privacy Mode ВЫКЛЮЧЕН** в BotFather
- Добавьте бота как администратора в чат

### Ошибки AI моделей
```bash
# Проверьте доступные модели
/models

# Добавьте резервную модель
/add_model backup deepseek/deepseek-chat:free
```

### Проблемы с Docker
```bash
# Перезапуск контейнера
docker-compose restart telegram-bot

# Полная пересборка
docker-compose down
docker-compose up -d --build

# Очистка данных (при проблемах с БД)
docker-compose down -v
docker-compose up -d
```

### Бот не публикует по расписанию
- Проверьте часовой пояс контейнера
- Убедитесь что бот работает: `docker-compose ps`
- Проверьте логи: `docker-compose logs telegram-bot`

## 📊 Мониторинг работы

### Проверка статуса
```bash
docker-compose ps
docker-compose logs telegram-bot --tail=50
```

### Статистика
- Используйте `/showconfig` для просмотра конфигурации
- В логах отображается количество обработанных сообщений
- База данных автоматически очищает сообщения старше 7 дней

## 🤖 Автоматический установщик

### Возможности установщика:

| Функция                     | Описание                                    |
|-----------------------------|---------------------------------------------|
| **Авто-установка Docker**   | Проверяет и устанавливает Docker если нужно |
| **Обновление репозитория**  | Автоматически клонирует или обновляет код   |
| **Интерактивная настройка** | Пошагово настраивает .env файл              |
| **Запуск сервисов**         | Собирает и запускает Docker контейнеры      |

### Использование:
```bash
./install-bot.sh install    # Полная установка
./install-bot.sh update     # Обновление
./install-bot.sh help       # Справка
```

**Что делает скрипт:**
1. 🔍 Проверяет установлен ли Docker
2. 📥 Устанавливает Docker если необходимо  
3. 🗂️ Клонирует репозиторий
4. ⚙️ Интерактивно настраивает .env файл
5. 🐳 Запускает Docker контейнеры
6. ✅ Проверяет что сервисы работают

___

**Техническое задание:** [Google Docs](https://docs.google.com/document/d/11Laxo2KT7Xx9FZCkh2ak6xFD6Jv6UVpcXOI9dbEbnrI/edit)

**NotebookLM:** [Google NotebookLM](https://notebooklm.google.com/notebook/a44a3b4c-ca6a-45dd-9e72-f53b9ac721aa)